#!/usr/bin/env gradle
// Gradle wrapper generation script
// Run with: gradle wrapper --gradle-version=8.6

tasks.register("generateWrapper", Wrapper) {
    gradleVersion = '8.6'
    distributionType = Wrapper.DistributionType.BIN
}
